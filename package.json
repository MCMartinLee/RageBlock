{
  "name": "rageblock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "e2e": "playwright test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "npm test && npm run typecheck && npm run e2e"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  }
}
